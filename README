b64 - base64 encoder and decoder, reads from files and writes to stdout 
author: Eugene Ma (edma2)
TODO: Get rid of two (mysterious) extra bytes encoded at the end of each file

Run 'make' in your shell.
